<?xml version="1.0"?>
<odoo>
    <!-- complaint resolve views and action -->
    <record id="crede_complaint_resolve_wizard_form_view" model="ir.ui.view">
        <field name="name">crede.complaint.resolve.wizard.form</field>
        <field name="model">crede.complaint.resolve.wizard</field>
        <field name="arch" type="xml">
            <form string="Complaint">
                <group>
                    <group>
                        <field name="priority" widget="priority" />
                        <field name="res_comment" placeholder="Enter Resolve Comment" />
                    </group>
                </group>
                <footer>
                    <button name="action_ok" string="Ok" type="object" class="btn-primary" />

                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>
    <record id="crede_complaint_resolve_wizard_view_actions" model="ir.actions.act_window">
        <field name="name">resolve Comment</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">crede.complaint.resolve.wizard</field>
        <field name="view_mode">form</field>
    </record>
</odoo>
